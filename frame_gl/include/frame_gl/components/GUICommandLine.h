#pragma once
#include "frame/Component.h"
#include "frame_gl/components/GUIRect.h"

namespace frame_gl
{
    FRAME_COMPONENT(GUICommandline, GUIRect) {
    public:
        GUICommandline() {}
        ~GUICommandline() {}

    private:
        
    }
}